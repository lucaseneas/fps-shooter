# Copie para .env.local e ajuste se quiser testar o build apontando para produção.
# Em dev local, deixe vazio — o cliente usa ws://localhost:2567 automaticamente.

# URL do Web Service Colyseus no Render (HTTPS; o colyseus.js converte para wss://):
VITE_SERVER_URL=https://fps-shooter-api.onrender.com
